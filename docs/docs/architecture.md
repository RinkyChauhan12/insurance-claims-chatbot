# System Architecture

The Insurance Claims Chatbot uses compressed insurance policy rules and summarized claim history to reduce context size and improve response accuracy.

User inputs are validated against compressed policy limits before generating claim eligibility results.
