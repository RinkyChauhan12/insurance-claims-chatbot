# Compression Strategy

Instead of passing full insurance documents to the AI model, only essential rules such as coverage limits, exclusions, and approval patterns are used.

This significantly reduces token usage and processing cost while improving accuracy.
